Public Class LeftSideModule
    Private Sub PictureBox1_Click(sender As Object, e As EventArgs) Handles PictureBox1.Click
        MessageBox.Show("Change the display status of the cells in the counter")

        'Me.ParentForm.
        'Call a funtion on parentForm that dumps all the cell modules and rebuilds them.
        'OR
        'say this change will take effect once the counter is closed and reopened.

    End Sub
End Class
